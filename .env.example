# Copy to .env (git-ignored). Restart `npm run dev` after changing.

# Remote sync endpoint. When set, the POS pushes queued sales here (see
# server/ for the Oracle backend). Leave commented out to stay in stub mode.
# VITE_SYNC_URL=http://localhost:3001/pos/sales
